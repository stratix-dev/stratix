import type { Command } from '@stratix/core';

export interface {{pascalCase name}}Command {
{{#each props}}
  {{name}}: {{type}};
{{/each}}
}

export class {{pascalCase name}} implements Command {
  constructor(public readonly data: {{pascalCase name}}Command) {}
}