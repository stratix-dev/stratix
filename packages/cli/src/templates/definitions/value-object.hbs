import { ValueObject } from '@stratix/core';

export interface {{pascalCase name}}Props {
{{#each props}}
  {{name}}: {{type}};
{{/each}}
}

export class {{pascalCase name}} extends ValueObject {
  private constructor(
{{#each props}}
    private readonly _{{name}}: {{type}}{{#unless @last}},{{/unless}}
{{/each}}
  ) {
    super();
  }

  static create(props: {{pascalCase name}}Props): {{pascalCase name}} {
    // TODO: Add validation logic here
    return new {{pascalCase name}}(
{{#each props}}
      props.{{name}}{{#unless @last}},{{/unless}}
{{/each}}
    );
  }
{{#each props}}

  get {{name}}(): {{type}} {
    return this._{{name}};
  }
{{/each}}

  protected getEqualityComponents(): unknown[] {
    return [
{{#each props}}
      this._{{name}}{{#unless @last}},{{/unless}}
{{/each}}
    ];
  }
}