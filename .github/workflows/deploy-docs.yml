# DISABLED: docs/website directory does not exist
# This workflow will be re-enabled when documentation structure is set up
name: Deploy Docusaurus to GitHub Pages (DISABLED)

on:
  # Disabled - uncomment when docs/website is ready
  workflow_dispatch:
  # push:
  #   branches:
  #     - main
  #   paths:
  #     - 'docs/website/**'
  #     - 'packages/*/src/**/*.ts'
  #     - 'typedoc.json'
  #     - 'typedoc-theme.css'
  #     - '.github/workflows/deploy-docs.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies (root)
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build

      - name: Generate API documentation
        run: pnpm docs:api

      - name: Install dependencies (website)
        working-directory: docs/website
        run: pnpm install --frozen-lockfile

      - name: Build website
        working-directory: docs/website
        run: pnpm build

      - name: Copy API docs to website build
        run: |
          mkdir -p docs/website/build/api
          cp -r docs/api/* docs/website/build/api/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/website/build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
