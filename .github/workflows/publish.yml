name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to publish (leave empty for latest)'
        required: false
        type: string

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm -r --filter '!website' build

      - name: Run tests
        run: pnpm test
        env:
          CI: true

      - name: Prepare for publish
        run: node scripts/prepare-publish.js

      - name: Verify package contents
        run: |
          echo "Verifying package contents..."

          # Function to verify a package
          verify_package() {
            local pkg_dir=$1
            if [ -f "$pkg_dir/package.json" ]; then
              echo "=== Verifying: $pkg_dir ==="
              cd "$pkg_dir"

              # Check for required files
              if [ ! -f "dist/index.js" ] && [ ! -f "dist/index.d.ts" ]; then
                echo "Warning: Missing dist files in $pkg_dir"
              fi

              if [ ! -f "README.md" ]; then
                echo "Warning: Missing README.md in $pkg_dir"
              fi

              # Show package.json info
              echo "Name: $(node -p "require('./package.json').name")"
              echo "Version: $(node -p "require('./package.json').version")"

              cd - > /dev/null
            fi
          }

          # Verify core packages
          verify_package "packages/core"
          verify_package "packages/runtime"
          verify_package "packages/testing"

          # Verify AI providers
          verify_package "packages/ai/providers/anthropic"
          verify_package "packages/ai/providers/openai"

          # Verify configuration providers
          verify_package "packages/providers/configuration/composite"
          verify_package "packages/providers/configuration/env"
          verify_package "packages/providers/configuration/file"

          # Verify DI providers
          verify_package "packages/providers/di/awilix"

          # Verify validation providers
          verify_package "packages/providers/validation/zod"

      - name: Publish packages
        run: |
          set -e  # Exit on any error
          
          # Configure npm authentication
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc

          # Track published packages
          PUBLISHED_PACKAGES=()
          FAILED_PACKAGES=()

          # Function to publish a package
          publish_package() {
            local pkg_dir=$1
            local original_dir=$(pwd)
            
            if [ ! -f "$pkg_dir/package.json" ]; then
              echo "⚠️  No package.json found in $pkg_dir, skipping..."
              return 0
            fi

            echo "📦 Processing $pkg_dir..."
            cd "$pkg_dir" || {
              echo "❌ Failed to change directory to $pkg_dir"
              FAILED_PACKAGES+=("$pkg_dir (cd failed)")
              cd "$original_dir"
              return 1
            }

            # Check if package is private
            local is_private=$(node -p "require('./package.json').private || false")
            if [ "$is_private" = "true" ]; then
              echo "⏭️  Skipping private package: $pkg_dir"
              cd "$original_dir"
              return 0
            fi

            # Get package name and version
            local pkg_name=$(node -p "require('./package.json').name")
            local pkg_version=$(node -p "require('./package.json').version")

            # Check if this version is already published
            if npm view "$pkg_name@$pkg_version" version 2>/dev/null; then
              echo "✓ Version $pkg_version of $pkg_name is already published, skipping..."
              cd "$original_dir"
              return 0
            fi

            echo "🚀 Publishing $pkg_name@$pkg_version..."

            # Publish to npm with error handling
            if npm publish --access public 2>&1; then
              echo "✅ Successfully published $pkg_name@$pkg_version"
              PUBLISHED_PACKAGES+=("$pkg_name@$pkg_version")
              cd "$original_dir"
              return 0
            else
              local exit_code=$?
              echo "❌ Failed to publish $pkg_name@$pkg_version (exit code: $exit_code)"
              FAILED_PACKAGES+=("$pkg_name@$pkg_version")
              cd "$original_dir"
              return 1
            fi
          }

          echo "========================================="
          echo "Starting package publication process"
          echo "========================================="
          echo ""

          # Publish in dependency order
          # 1. Core packages (no dependencies)
          publish_package "packages/core" || true

          # 2. Runtime (depends on core)
          publish_package "packages/runtime" || true

          # 3. Providers
          # DI provider
          publish_package "packages/providers/di/awilix" || true

          # AI providers
          publish_package "packages/ai/providers/anthropic" || true
          publish_package "packages/ai/providers/openai" || true

          # Validation providers
          publish_package "packages/providers/validation/zod" || true

          # Configuration providers
          publish_package "packages/providers/configuration/composite" || true
          publish_package "packages/providers/configuration/env" || true
          publish_package "packages/providers/configuration/file" || true

          # 4. Testing utilities (depend on core, runtime)
          publish_package "packages/testing" || true

          echo ""
          echo "========================================="
          echo "Publication Summary"
          echo "========================================="
          echo ""
          
          if [ ${#PUBLISHED_PACKAGES[@]} -gt 0 ]; then
            echo "✅ Successfully published (${#PUBLISHED_PACKAGES[@]} packages):"
            for pkg in "${PUBLISHED_PACKAGES[@]}"; do
              echo "  - $pkg"
            done
            echo ""
          fi

          if [ ${#FAILED_PACKAGES[@]} -gt 0 ]; then
            echo "❌ Failed to publish (${#FAILED_PACKAGES[@]} packages):"
            for pkg in "${FAILED_PACKAGES[@]}"; do
              echo "  - $pkg"
            done
            echo ""
            echo "⚠️  Some packages failed to publish. Please check the logs above."
            exit 1
          fi

          if [ ${#PUBLISHED_PACKAGES[@]} -eq 0 ] && [ ${#FAILED_PACKAGES[@]} -eq 0 ]; then
            echo "ℹ️  No new packages to publish (all versions already published)"
          fi

        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create publish summary
        if: success()
        run: |
          echo "## Published Packages" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Function to add package to summary
          add_to_summary() {
            local pkg_dir=$1
            if [ -f "$pkg_dir/package.json" ]; then
              local name=$(node -p "require('./$pkg_dir/package.json').name")
              local version=$(node -p "require('./$pkg_dir/package.json').version")
              echo "- \`$name@$version\`" >> $GITHUB_STEP_SUMMARY
            fi
          }

          echo "### Core" >> $GITHUB_STEP_SUMMARY
          add_to_summary "packages/core"
          add_to_summary "packages/runtime"

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### AI Providers" >> $GITHUB_STEP_SUMMARY
          add_to_summary "packages/ai/providers/anthropic"
          add_to_summary "packages/ai/providers/openai"

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### DI Providers" >> $GITHUB_STEP_SUMMARY
          add_to_summary "packages/providers/di/awilix"

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Validation Providers" >> $GITHUB_STEP_SUMMARY
          add_to_summary "packages/providers/validation/zod"
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Configuration Providers" >> $GITHUB_STEP_SUMMARY
          add_to_summary "packages/providers/configuration/composite"
          add_to_summary "packages/providers/configuration/env"
          add_to_summary "packages/providers/configuration/file"

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Testing" >> $GITHUB_STEP_SUMMARY
          add_to_summary "packages/testing"

      - name: Notify on failure
        if: failure()
        run: |
          echo "## Publish Failed" >> $GITHUB_STEP_SUMMARY
          echo "Check the logs for details." >> $GITHUB_STEP_SUMMARY
